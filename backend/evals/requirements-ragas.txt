# Dependencies for `evals/ragas_eval.py` ONLY, install into a separate environment; see docs/architecture.md's 2026-07-28 entry for why.

# Setup: venv OUTSIDE the repo (short path), python -m venv C:\venvs\esg-ragas, then pip install -r this file.

# RAGAS itself, pinned exactly (not floored) to the combination verified working on 2026-07-28.
ragas==0.2.15
langchain-openai==0.2.14
langchain-core==0.3.86
langchain-community==0.3.31

# The application's own runtime dependencies, mirrored by hand from pyproject.toml's [project].dependencies.
# `openai` is capped below 2 here only, because langchain-openai 0.2.x requires it; the app itself stays on openai 2.x.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.4
pypdf>=5.1
pdfplumber>=0.11
pymupdf>=1.24
openai>=1.50,<2
httpx>=0.27
pyjwt[crypto]>=2.9
qdrant-client>=1.12
sentence-transformers>=3.0
langfuse>=4.14.1
